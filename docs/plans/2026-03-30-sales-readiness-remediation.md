# 2026-03-30 正式销售前整改执行记录

## 这次实际落地的内容

### 后端

- 激活码生成改成系统级安全随机数，继续保持 `XXXX-XXXX-XXXX-XXXX` 可读格式
- 大纲修改次数改成数据库原子占位，避免并发修改钻空子
- 后端入口改成应用工厂，统一加基础安全中间件和跨域白名单
- `targetWords`、`editInstruction`、任务列表 `status` 参数补上服务端校验
- 系统配置增加短时缓存，`setConfig` 后自动清缓存
- 增加结构化审计日志骨架
- 增加可选错误监控骨架，默认不开，填了 `SENTRY_DSN` 才上报

### 前端

- 去掉真实 Supabase 地址和公开 key 的源码兜底值，统一改从环境变量读取
- 请求工具补齐状态码检查和非 JSON 容错
- 登录增加本地输错冷却保护
- 注册密码规则提升到“至少 8 位，且同时包含字母和数字”
- 工作台轮询改成“先快后慢 + 总超时”
- 前端静态服务继续统一输出基础安全头

### 工具链

- 新增依赖安全检查 GitHub 工作流

## 这次没有扩大的范围

- 不改收费规则
- 不改数据库主要业务结构
- 不重新设计前端页面
- 不对已经由数据库函数兜底的扣费动作再额外叠一层重复机制

## 还需要继续确认的线上项

- Railway `app` 和 `cleanup` 要补齐 `ALLOWED_ORIGINS`
- Railway `app` 和 `cleanup` 要补齐 `CONFIG_CACHE_TTL_MS`
- 如果要启用错误监控，再补 `SENTRY_DSN`
- Supabase 需要执行新增的审计日志和大纲修改原子操作迁移
